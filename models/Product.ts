export interface Product {
    id: number,
    title: string,
    description: string,
    brand: string,
    category: string,
    price: string,
    rating: string,
    stock: string,
    thumbnail: string,
    images: string[]
}